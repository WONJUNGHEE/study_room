export type User = {
  id: number;
  name: string;
};

export interface sch {
  using: boolean;
  name: string;
  phone: string;
  count: string;
  time?: string;
}
export interface login {
  id: string;
}
